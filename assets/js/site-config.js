/**
 * site-config.js — tells the public front page which GitHub repo and
 * branch to read published articles from. Visitors never log in; this
 * is a fixed pointer you set once.
 *
 * EDIT THE THREE VALUES BELOW to match your own repository, then the
 * front page will pull live content from it automatically, with no
 * build step. If you're using the same repo that hosts this site (the
 * common case), owner/repo/branch usually match where you pushed this
 * very file.
 */

window.SITE_REPO = {
  owner: 'theragajournal,
  repo: 'theragajournal,
  branch: 'main',
};
